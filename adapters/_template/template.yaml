# Template Adapter Configuration
# Copy and modify for your vendor

adapter:
  type: template
  vendor: template
  robot_id: template-001

connection:
  base_url: http://localhost:8080
  timeout: 10
  retry_count: 3

polling:
  interval_seconds: 2

mqtt:
  broker: localhost
  port: 1883
